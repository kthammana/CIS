import numpy as np
from Point3d import Point3d

def calcDistortionCorrection(C_exp, C):

    return coef # return something random for now


def applyDistortionCorrection(X, coef):

    return X_corr # return corrected C values